name: Lint

on:
  push:
    branches: [ main, master, develop, 'claude/**' ]  # Allow claude/* branches
  pull_request:
    branches: [ main, master, develop ]

jobs:
  lint:
    name: Lint with ruff and mypy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install only linting tools, not full requirements.txt
        pip install mypy==1.13.0 ruff==0.8.4 black==24.1.1
        pip install numpy==2.3.4 requests==2.32.5  # For type checking imports

    - name: Run ruff linter
      run: |
        python -m ruff check clockify_support_cli_final.py --config pyproject.toml
      continue-on-error: true

    - name: Run mypy type checker
      run: |
        python -m mypy clockify_support_cli_final.py --config-file pyproject.toml
      continue-on-error: true

  format-check:
    name: Check code formatting
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install black
      run: |
        python -m pip install --upgrade pip
        pip install black==24.1.1

    - name: Check formatting with black
      run: |
        python -m black --check --diff clockify_support_cli_final.py --config pyproject.toml
      continue-on-error: true
